import styled from "styled-components"

const Container = styled.div`
  width: 100%;
`

const Map = () => {
  return (
    <Container>
      <span>@TODO: Replace placeholder with map</span>
    </Container>
  )
}

export default Map
