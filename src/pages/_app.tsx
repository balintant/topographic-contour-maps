import '@/styles/globals.css'
import type { AppProps } from 'next/app'

function TopographicContourMapApp({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />
}

export default TopographicContourMapApp
